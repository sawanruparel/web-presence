# Environment Variables Template
# Copy this to .dev.vars (for api/) and .env.local (for web/) and fill in values

# ============================================================
# Backend API (/api/.dev.vars)
# ============================================================

# API Key for internal endpoints (build script, admin operations)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
INTERNAL_API_KEY=your-random-64-char-hex-string-here

# JWT Secret for token signing
JWT_SECRET=your-jwt-secret-change-in-production

# ============================================================
# Frontend Build (/web/.env.local)
# ============================================================

# Backend API URL for build script
BUILD_API_URL=http://localhost:8787

# API Key - must match INTERNAL_API_KEY from api/.dev.vars
BUILD_API_KEY=your-random-64-char-hex-string-here

# ============================================================
# Instructions
# ============================================================

# 1. Generate API key:
#    node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# 2. Create api/.dev.vars with INTERNAL_API_KEY
#
# 3. Create web/.env.local with same key as BUILD_API_KEY
#
# 4. Never commit these files to git!
