# Cloudflare Workers environment variables for local development
# Copy this file to .dev.vars and update the values

# Internal API Key for admin operations (build script, admin operations)
INTERNAL_API_KEY=your-internal-api-key-here

# Frontend URL for CORS and references (local development)
FRONTEND_URL=http://localhost:5173

# CORS Origins (comma-separated list of allowed origins)
CORS_ORIGINS=http://localhost:5173,http://localhost:5174,http://localhost:3000,http://127.0.0.1:5173,http://127.0.0.1:5174

# GitHub Integration (for content sync and management)
GITHUB_TOKEN=your-github-token-here
GITHUB_WEBHOOK_SECRET=your-github-webhook-secret-here
GITHUB_REPO=your-username/your-repo-name
GITHUB_BRANCH=main

# R2 Bucket Names (these should match your actual bucket names)
# Note: The actual R2 bucket bindings are configured in wrangler.toml
# These variables are for reference and any additional configuration
PROTECTED_CONTENT_BUCKET_NAME=protected-content
PUBLIC_CONTENT_BUCKET_NAME=public-content

# Admin Password (for admin panel authentication)
ADMIN_PASSWORD=all-content

# Note: DATABASE_ID is configured in wrangler.toml
# Note: NODE_ENV is handled by Cloudflare Workers automatically
